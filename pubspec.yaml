name: _
publish_to: none

environment:
  sdk: '^3.8.0'

workspace:
  - turn_tracker_pro_client
  - turn_tracker_pro_server
  - turn_tracker_pro_flutter
dependency_overrides:
  flutter_secure_storage: ^10.0.0

